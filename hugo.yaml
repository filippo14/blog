baseURL: "http://localhost:1313/"
languageCode: "en-us"
title: "Filippo Moret"
theme: "PaperMod"
enableRobotsTXT: true # Ensures robots.txt is generated

# Minification settings - can be enabled for production
# minify:
#   disableXML: true
#   minifyOutput: true

params:
  # env: production # HUGO_ENV=production is set in GitHub Actions workflow for deployments
  author: "Filippo Moret"
  description: "A blog about AI, SRE, and technology."
  keywords: ["AI", "SRE", "technology", "infrastructure", "blog", "software", "Filippo Moret"]
  # images: ["/path/to/your/default-social-image.jpg"] # Site-wide image for OpenGraph/Twitter if no page image

  defaultTheme: "auto"
  enableArchive: true
  # dateFormat: "January 2, 2006"
  ShowReadingTime: false
  ShowShareButtons: true # Enable for SEO (social signals)
  ShareButtons: ["linkedin", "twitter", "reddit", "whatsapp", "telegram", "facebook"]
  ShowPostNavLinks: true
  ShowBreadCrumbs: true # Good for SEO and UX
  ShowCodeCopyButtons: true
  ShowWordCount: false
  ShowRssButtonInSectionTermList: true # Good for discoverability
  ShowToc: false 
  TocOpen: false

  # Favicon and Apple Touch Icons (place these in your static/ folder)
  # assets:
  #   favicon: "/favicon.ico"
  #   favicon16x16: "/favicon-16x16.png"
  #   favicon32x32: "/favicon-32x32.png"
  #   apple_touch_icon: "/apple-touch-icon.png"
  #   safari_pinned_tab: "/safari-pinned-tab.svg"

  homeInfoParams:
    Title: "Ciao 👋"
    Content: "This is my personal corner of the internet. Expect deep dives, curious and fictional conversations about the evolving world of AI, and thoughts from an SRE-ish perspective."

  socialIcons:
    - name: linkedin
      url: "https://www.linkedin.com/in/filippomoret/"
    # - name: github
    #   url: "YOUR_GITHUB_URL"

  label:
    text: "Filippo Moret"

  mainSections: "posts"

menu:
  main:
    - identifier: "archive"
      name: "Archive"
      url: "/posts/"
      weight: 10
    - identifier: "search"
      name: "Search"
      url: "/search/"
      weight: 20
    - identifier: "tags"
      name: "Tags"
      url: "/tags/"
      weight: 30
    - identifier: "about"
      name: "About"
      url: "/about/"
      weight: 40

outputs:
  home: ["HTML", "RSS", "JSON"]
  section: ["HTML", "RSS", "JSON"]
  taxonomy: ["HTML", "RSS", "JSON"]
  term: ["HTML", "RSS", "JSON"]

# services:
#   googleAnalytics:
#     ID: "YOUR_GA_MEASUREMENT_ID" # Replace with your GA4 Measurement ID

# For the homepage to list posts from 'content/posts/'
